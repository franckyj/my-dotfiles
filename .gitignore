# Wallpapers are cloned from wallpapers.git at install time (see install.sh),
# not shipped in this repo — matches the other *-setup installers.
oxwm/wallpaper/

# Active theme state: generated by scripts/thememenu, seeded by install.sh
# from themes/github_dark/. Not tracked so theme switches don't dirty the repo.
oxwm/colors.lua
oxwm/rofi/colors.rasi
